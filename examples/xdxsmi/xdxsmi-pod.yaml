apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  restartPolicy: OnFailure
  containers:
  - image: ubuntu:20.04
    name: pod-xdxsmi
    command: ["xdxsmi"]

    resources:
      limits:
        xdxct.com/gpu: 1

